# TELEGRAM SETUP FUER BEN
# Wenn Ben den Bot-Token hat, diese Zeilen in ~/.hermes/.env eintragen:
# TELEGRAM_BOT_TOKEN=DEIN_TOKEN_HIER
# TELEGRAM_ALLOWED_USERS=DEINE_TELEGRAM_USER_ID
#
# Schritt 1: @BotFather auf Telegram oeffnen
# Schritt 2: /newbot eingeben
# Schritt 3: Namen waehlen (z.B. BensHermesBot)
# Schritt 4: Token kopieren und in .env eintragen
# Schritt 5: /mybots > Bot auswaehlen > API Token kopieren
# Schritt 6: User-ID herausfinden (@userinfobot)
# Schritt 7: User-ID in TELEGRAM_ALLOWED_USERS eintragen
# Schritt 8: hermes restart
